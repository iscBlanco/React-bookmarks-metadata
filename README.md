# React-bookmarks-metadata
Web scraping metadata for adding bookmarks of websites

*** The full project is located bookmark-metadata folder ****

SOLO FUNCIONA CON EL URL DE http://bedu.org

scraping is done in /container/GetUrlHtml.js file and  /ui-presentation/FormUrl.js calls it.

state management is done with Context API

-Falto la ventana popup modal

the commits don't appear because i work with GIT FLOW and I merge the branches into master

